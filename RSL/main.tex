% Draft for Remote Sensing Letters (Taylor & Francis).
% Before submission, move the body into the T&F "Interact" LaTeX template
% (https://www.tandfonline.com/action/authorSubmission?journalCode=trsl20)
% and check the current word / figure limits for Letters.
%
% Format requirements, read from the journal's Instructions for Authors page
% (tandfonline.com/action/authorSubmission?show=instructions&journalCode=trsl20,
% 6 Sep 2026): Research Letter <= 12 pages inclusive of tables, references and
% captions; unstructured abstract of 200 words; keywords; order: title page,
% abstract, keywords, introduction, materials and methods, results, discussion,
% acknowledgments, disclosure statement, references; declaration of generative
% AI use, funding details and data availability statement required; British
% -ize spelling; single-spaced with 3 cm margins; T&F Chicago author-date
% references; LaTeX (Interact template) accepted, submit PDF + zipped source,
% both an anonymous and a with-author-details manuscript; figures 300 dpi
% colour, supplied separately at revision (JPEG/TIFF/PS preferred).
%
\documentclass[11pt]{article}
\usepackage[a4paper,margin=3cm]{geometry}
\usepackage{amsmath,amssymb}
\usepackage{graphicx}
\usepackage{booktabs}
\usepackage{url}
\usepackage{xcolor}
\usepackage[hidelinks]{hyperref}
\usepackage{setspace}
\usepackage[round]{natbib}
\bibpunct{(}{)}{,}{a}{}{,}
\singlespacing
\newcommand{\tbd}[1]{\textcolor{red}{[#1]}}
% Build the anonymous review copy with:  pdflatex "\def\anonymous{1}\input{main.tex}"  (see main_anon.tex)
\ifdefined\anonymous
  \newcommand{\repourl}{[repository URL withheld for anonymous review; released on acceptance]}
  \newcommand{\authorblock}{Anonymous author}
\else
  \newcommand{\repourl}{\url{https://github.com/hz6yc3/attention-enhanced-unet-segmentation}}
  \newcommand{\authorblock}{Anantharaman Janakiraman\\[3pt]
    \normalsize Department of Electrical Engineering and Computer Science\\
    \normalsize Florida Atlantic University, Boca Raton, FL, USA\\[3pt]
    \normalsize Corresponding author: \texttt{ajanakiraman2024@fau.edu}\\
    \normalsize ORCID: \href{https://orcid.org/0009-0008-3641-0788}{0009-0008-3641-0788}}
\fi

\title{Selecting GAN-Synthetic Training Images by Committee Disagreement for Small-Data Satellite Road Segmentation}
\author{\authorblock}
\date{}

\begin{document}
\maketitle

\begin{abstract}
GAN-generated images are increasingly bundled with small annotated satellite datasets, but their value for road extraction is rarely measured cleanly. Auditing a public road-segmentation dataset (100 real images plus 1{,}003 Pix2Pix-generated pairs), we find only 106 distinct synthetic images and 75 distinct masks, each file repeated about twelve times. Under a protocol with a fixed real test set, five-fold real-only cross-validation, synthetic data confined to training, matched optimizer budgets and 15 paired replicates per condition, the synthetic images raise Attention U-Net test Dice from 0.806 to 0.839 once duplicates are removed; training on the 1{,}003 files as shipped is significantly worse than training on the 106 unique images, and validating on the pooled real--synthetic set inflates the apparent benefit about sevenfold. We then select synthetic images by the disagreement of a committee of real-trained models. On the duplicated set every score-based selection collapses to 26 unique images and loses to random sampling. After deduplication, the 50 most-disputed images match the full unique set, exceed a random 50 in 11 of 15 replicates, beat the 50 most-agreed images by 2.6 Dice points and give the lowest seed sensitivity of any condition. Deduplication, real-only evaluation and disagreement-based selection are recommended.
\end{abstract}

\noindent\textbf{Keywords:} road extraction; synthetic data; Pix2Pix; data duplication; evaluation leakage; query by committee; Attention U-Net

\section{Introduction}

Pixel-level road annotation is expensive, and conditional generative models offer a shortcut: Pix2Pix \citep{isola2017} translates a road mask into a plausible satellite image, so new mask layouts give new training pairs with perfect label alignment. Some public datasets, including the one studied here, bundle such GAN-generated training images with the real ones, and studies that use them have reported large improvements. Practitioners nevertheless face three unanswered questions. How much of a synthetic set is genuinely new content? How much real-image benefit does it provide once the evaluation is clean? And can the useful synthetic images be selected in advance?

We answer all three for the Kaggle dataset \emph{Satellite Images for Road Segmentation} \citep{sanadalali}, which ships 100 annotated satellite images and 1{,}003 Pix2Pix-generated pairs. The work was motivated by a preliminary experiment of our own on this dataset in which, following common practice, the validation split was drawn from the pooled real and synthetic images: Attention U-Net Dice appeared to rise from 0.81 to 0.96 when the synthetic images were added. Two questions about that result, whether the pooled validation split was measuring performance on real imagery at all, and whether the synthetic set contained as much distinct content as its size suggested, turned out to have answers that are more useful than the original number.

The contributions are as follows. (i) A file-level audit shows that the 1{,}003 synthetic pairs contain 106 distinct images and 75 distinct masks, each duplicated about twelve times. (ii) Under a leakage-free, compute-matched protocol with 15 paired replicates per condition, we measure the real benefit of the synthetic data on held-out real images (3.3 Dice points from the 106 unique images), show that training on the duplicated files as shipped is significantly worse than training on the unique images, and quantify the sevenfold inflation caused by validating on the pooled set. (iii) We show that query-by-committee selection of synthetic images fails on the duplicated set, because identical files receive identical scores and any ranking collapses to a few unique images, but succeeds after deduplication: the 50 most-disputed unique images match the full unique set and outperform random and agreement-based selections, with the lowest seed sensitivity of any condition. Code, the frozen split file, committee scores and all per-run results are released.\footnote{\repourl}

\section{Related work}

U-Net \citep{ronneberger2015} and its variants dominate road extraction from overhead imagery \citep{zhang2018,zhou2018,batra2019}. Attention U-Net \citep{oktay2018} adds additive attention gates to the skip connections, a modification of about 1\% more parameters that suppresses background features; it is the architecture used throughout. GAN-based augmentation \citep{goodfellow2014,isola2017} is widely used against annotation scarcity in remote sensing, but evaluation practice is inconsistent and gains are often measured on splits that include synthetic images. Duplicate and near-duplicate contamination has been documented in standard vision benchmarks, where copies shared between training and test sets inflate test accuracy \citep{barz2020}; to our knowledge it has not been reported for a GAN-expanded remote-sensing training set, where its effect is different: it inflates the \emph{apparent size} of the training set and defeats per-file selection rules. Query-by-committee \citep{seung1992,settles2009} selects unlabelled samples on which independently trained models disagree; its use for choosing among \emph{synthetic} samples, whose labels are fixed by construction, has not been evaluated on a segmentation task with a real held-out test set.

\section{Materials and methods}

\subsection{Data audit}

The dataset provides 100 real RGB satellite images of $400\times400$ pixels with binary road masks (road fraction $20.3\%\pm7.2\%$), 50 unlabelled test images that we do not use, and 1{,}003 $416\times416$ image--mask pairs that the dataset page describes as generated with Pix2Pix. The provenance of the synthetic masks is not documented on the dataset page. Figure~\ref{fig:dataset} shows real samples.

Hashing every synthetic file reveals that the set is heavily duplicated. There are \textbf{106 distinct synthetic images} and \textbf{75 distinct synthetic masks}: 45 masks were rendered once, 29 twice and one three times, and every resulting image was then copied, byte for byte, between 1 and 14 times (median 12). Every copy of an image carries the same mask. None of the 75 synthetic masks is pixel-identical to a real mask, or to a flipped or rotated real mask, after resizing the real masks to the synthetic resolution; we did not search for near-duplicates of real masks. The synthetic set therefore does not copy real test images outright, but its effective size is about a tenth of its nominal size. Table~\ref{tab:audit} summarizes the audit. Two consequences follow for training: ``1{,}003 synthetic images'' is a twelve-fold oversampling of 106, and any per-file selection rule will select whole groups of identical copies.

\begin{table}[t]
\centering
\caption{Audit of the 1{,}003 synthetic image--mask pairs.}
\label{tab:audit}
\small
\begin{tabular}{lr}
\toprule
Synthetic files & 1{,}003 \\
Distinct images (byte-identical copies collapsed) & 106 \\
Distinct masks & 75 \\
Copies per distinct image: min / median / max & 1 / 12 / 14 \\
Distinct images per mask: one / two / three & 45 / 29 / 1 \\
Synthetic masks pixel-identical to a (flipped/rotated) real mask & 0 \\
Distinct committee-score pairs (fold 0) & 93 \\
\bottomrule
\end{tabular}
\end{table}

\begin{figure}[t]
\centering
\includegraphics[width=0.85\linewidth]{fig_dataset.png}
\caption{Real training samples spanning urban, suburban and rural scenes (top) and their binary road masks (bottom). Source: the publicly available Kaggle dataset \emph{Satellite Images for Road Segmentation} \citep{sanadalali}.}
\label{fig:dataset}
\end{figure}

\subsection{Evaluation protocol}

\paragraph{Real-only cross-validation.} The 100 real images are sorted by road fraction and stratified into a fixed \textbf{test set of 20} and \textbf{five folds of 16}; the assignment is written to a split file shared by every experiment and released with the code. For fold $f$, the 16 images of fold $f$ are the validation set, the other 64 are the real training set, and the 20 test images are untouched. Synthetic images are added to the training set only. Model selection uses the real validation fold; the test set is scored once per run with the selected weights.

\paragraph{Compute-matched training.} Adding 1{,}003 files multiplies the updates per epoch by 17. To separate the effect of data from the effect of more optimization, every run trains for exactly 1{,}200 optimizer steps at batch size 8 (Adam, learning rate $10^{-4}$, weight decay $10^{-5}$, 100 warm-up steps then cosine decay to $10^{-6}$) regardless of training-set size. Images are resized to $256\times256$ and normalized with ImageNet statistics; augmentation uses flips, $90^\circ$ rotations, shift--scale--rotate, brightness/contrast, hue/saturation and Gaussian noise \citep{buslaev2020}. The loss is binary cross-entropy plus Dice loss; mixed precision is used. The validation fold is evaluated every 100 steps and the best checkpoint by mean per-image Dice is kept. The Attention U-Net follows \citet{oktay2018} with encoder widths 64--128--256--512, a 1{,}024-channel bottleneck, transposed-convolution upsampling and attention gates on all four skip connections (31.4\,M parameters).

\paragraph{Replication and statistics.} Every condition is trained for all five folds with three seeds (42, 123, 456), giving 15 replicates; seeds control initialization, data order and augmentation. We report mean per-image Dice and IoU on the 20 test images, the standard deviation across replicates, and the mean over folds of the standard deviation across seeds (seed sensitivity). Conditions are compared with the Wilcoxon signed-rank test and the paired $t$-test over the 15 matched (fold, seed) pairs.

\subsection{Committee-based selection of synthetic images}

For each fold, the three real-only models form a committee. Each synthetic image is passed through the committee, predictions are binarized at 0.5, and the \emph{committee disagreement} is the mean pairwise $1-\mathrm{Dice}$ between the three predictions. The committee is trained only on the real training folds, so the score never sees validation or test images. For diagnosis we also record each member's $1-\mathrm{Dice}$ against the synthetic mask (\emph{label disagreement}) and the mask's road fraction. Identical files receive identical scores.

Two families of conditions are compared, all with the 64 real training images. On the \textbf{duplicated set} (files as shipped): \emph{all} (1{,}003 files); \emph{random} (250 files drawn uniformly, re-drawn per fold and seed); \emph{most-agreed} (250 lowest-disagreement files, i.e.\ filtering suspected artefacts); and \emph{most-disputed} (250 highest-disagreement files, i.e.\ uncertainty sampling). On the \textbf{deduplicated set} (one file per distinct image): \emph{all-unique} (106); and \emph{random}, \emph{most-agreed}, \emph{middle band} (the 50 images centred on the median score) and \emph{most-disputed}, each with 50 unique images. Together with the real-only baseline this is $10$ conditions $\times 15$ replicates $= 150$ training runs plus five scoring passes, about 15 GPU-hours on a single T4.

\section{Results}

\begin{table}[t]
\centering
\caption{Test results on the 20 held-out real images (mean $\pm$ SD over 15 fold$\times$seed replicates). ``Unique'' is the mean number of distinct synthetic images present in the training set. Seed sensitivity is the SD across seeds averaged over folds. All runs use 64 real images and an identical 1{,}200-step budget.}
\label{tab:main}
\footnotesize
\setlength{\tabcolsep}{4pt}
\begin{tabular}{llrrrrr}
\toprule
Set & Condition & Files & Unique & Dice & IoU & Seed sens. \\
\midrule
--           & Real only        & 0       & 0    & $0.806 \pm 0.015$ & $0.698 \pm 0.018$ & 0.014 \\
\midrule
Duplicated   & + all            & 1{,}003 & 106  & $0.828 \pm 0.010$ & $0.726 \pm 0.013$ & 0.009 \\
             & + random         & 250     & 81   & $0.832 \pm 0.011$ & $0.731 \pm 0.016$ & 0.012 \\
             & + most-agreed    & 250     & 26   & $0.803 \pm 0.007$ & $0.702 \pm 0.010$ & 0.007 \\
             & + most-disputed  & 250     & 26   & $0.798 \pm 0.021$ & $0.677 \pm 0.027$ & 0.017 \\
\midrule
Deduplicated & + all-unique     & 106     & 106  & $\mathbf{0.839 \pm 0.014}$ & $\mathbf{0.741 \pm 0.018}$ & 0.013 \\
             & + random         & 50      & 50   & $0.830 \pm 0.013$ & $0.733 \pm 0.015$ & 0.012 \\
             & + most-agreed    & 50      & 50   & $0.812 \pm 0.010$ & $0.712 \pm 0.011$ & 0.009 \\
             & + middle band    & 50      & 50   & $0.815 \pm 0.013$ & $0.716 \pm 0.019$ & 0.012 \\
             & + most-disputed  & 50      & 50   & $\mathbf{0.839 \pm 0.009}$ & $0.738 \pm 0.012$ & $\mathbf{0.007}$ \\
\bottomrule
\end{tabular}
\end{table}

\begin{table}[t]
\centering
\caption{Paired comparisons of test Dice over the 15 matched (fold, seed) replicates. $\Delta$ is the mean difference $a-b$ with 95\% confidence interval; ``wins'' counts replicates in which $a$ exceeds $b$. Numbers in parentheses are files (duplicated set) or unique images (deduplicated set, marked $\dagger$).}
\label{tab:paired}
\footnotesize
\setlength{\tabcolsep}{4pt}
\begin{tabular}{llrrrr}
\toprule
$a$ & $b$ & $\Delta$ Dice & Wins & Wilcoxon $p$ & Paired $t$ $p$ \\
\midrule
\multicolumn{6}{l}{\emph{Duplicated set}} \\
random (250)         & real only    & $+0.026 \pm 0.010$ & 13/15 & $3.1\times10^{-4}$ & $8.4\times10^{-5}$ \\
all (1{,}003)        & real only    & $+0.022 \pm 0.010$ & 14/15 & $1.2\times10^{-3}$ & $3.4\times10^{-4}$ \\
all (1{,}003)        & random (250) & $-0.004 \pm 0.005$ & 3/15  & 0.064 & 0.14 \\
most-agreed (250)    & random (250) & $-0.029 \pm 0.006$ & 0/15  & $6.1\times10^{-5}$ & $1.1\times10^{-7}$ \\
most-disputed (250)  & random (250) & $-0.033 \pm 0.011$ & 1/15  & $1.2\times10^{-4}$ & $1.3\times10^{-5}$ \\
\midrule
\multicolumn{6}{l}{\emph{Deduplicated set}} \\
all-unique (106)$^\dagger$   & all (1{,}003)           & $+0.011 \pm 0.007$ & 13/15 & $6.7\times10^{-3}$ & $4.7\times10^{-3}$ \\
all-unique (106)$^\dagger$   & real only               & $+0.033 \pm 0.014$ & 13/15 & $3.1\times10^{-4}$ & $1.3\times10^{-4}$ \\
random (50)$^\dagger$        & real only               & $+0.024 \pm 0.011$ & 13/15 & $8.5\times10^{-4}$ & $2.8\times10^{-4}$ \\
most-disputed (50)$^\dagger$ & real only               & $+0.033 \pm 0.007$ & 15/15 & $6.1\times10^{-5}$ & $7.1\times10^{-8}$ \\
most-disputed (50)$^\dagger$ & random (50)$^\dagger$   & $+0.008 \pm 0.009$ & 11/15 & 0.048 & 0.070 \\
most-disputed (50)$^\dagger$ & all-unique (106)$^\dagger$ & $-0.001 \pm 0.011$ & 10/15 & 0.80 & 0.90 \\
most-disputed (50)$^\dagger$ & most-agreed (50)$^\dagger$ & $+0.026 \pm 0.008$ & 14/15 & $1.2\times10^{-4}$ & $8.9\times10^{-6}$ \\
most-agreed (50)$^\dagger$   & random (50)$^\dagger$   & $-0.018 \pm 0.008$ & 2/15  & $6.1\times10^{-4}$ & $3.8\times10^{-4}$ \\
middle band (50)$^\dagger$   & random (50)$^\dagger$   & $-0.015 \pm 0.011$ & 4/15  & 0.018 & 0.010 \\
\bottomrule
\end{tabular}
\end{table}

\begin{figure}[t]
\centering
\includegraphics[width=0.9\linewidth]{fig_conditions.png}\\[2pt]
\includegraphics[width=0.9\linewidth]{fig_conditions_dedup.png}
\caption{Test Dice of every replicate per condition on the duplicated set as shipped (top) and after deduplication (bottom). Grey lines join the same (fold, seed) across conditions; black bars show mean $\pm$ SD. Both panels share the vertical scale.}
\label{fig:conditions}
\end{figure}

\begin{figure}[t]
\centering
\includegraphics[width=0.48\linewidth]{fig_paired.png}\hfill
\includegraphics[width=0.48\linewidth]{fig_paired_dedup.png}
\caption{Paired differences in test Dice relative to the random subset of the same family: 250 files on the duplicated set (left) and 50 unique images on the deduplicated set (right). Diamonds and bars show the mean and 95\% confidence interval over 15 replicates.}
\label{fig:paired}
\end{figure}

\paragraph{Synthetic data helps by about three Dice points, and the copies hurt.} Table~\ref{tab:main} and Figure~\ref{fig:conditions} summarize the 150 runs. Training on the 106 unique synthetic images raises test Dice from 0.806 to 0.839 ($+3.3$ points, 13/15 wins, Table~\ref{tab:paired}). Training on the 1{,}003 files as shipped reaches only 0.828: the same content with twelve-fold repetition is significantly worse than the unique images under an identical budget ($\Delta=-0.011$, 13/15, $p<0.01$), and no better than a random 250-file subset that happens to contain 81 unique images. Synthetic data also reduces seed sensitivity, from 0.014 to 0.009--0.013.

\paragraph{Evaluation on the pooled set inflates the benefit sevenfold.} In our preliminary experiment under a pooled-validation protocol (the 1{,}103 real and synthetic images shuffled together and 15\% held out for validation, three seeds), the same architecture and data showed a gain of 15.1 Dice points from synthetic expansion (mean validation Dice 0.810 to 0.961). Under the real-only protocol the gain is 2.2 points for the same 1{,}003 files. Two mechanisms combine: synthetic images are easier than real ones, having perfect label alignment and no annotation noise, and because each synthetic image appears twelve times, a validation split drawn from the pool almost always contains copies of images that are also in the training split, so the pooled protocol partly measures memorization.

\paragraph{On the duplicated set, committee selection collapses diversity and loses to random.} Because identical files receive identical scores, ranking by committee disagreement selects whole groups of copies: the 250 most-agreed files contain 26 unique images (range 24--27) and so do the 250 most-disputed, against 81 for a random draw. Both selections lose to random in nearly every replicate (mean paired difference $\Delta=-0.029$ and $-0.033$ Dice, 0/15 and 1/15 wins, $p<10^{-3}$) and neither differs significantly from using no synthetic data at all (Figure~\ref{fig:paired}, left). This is the realistic setting for a practitioner who downloads a GAN-expanded dataset and applies a per-image score, and the lesson is that any such score must be applied after deduplication.

\paragraph{After deduplication, disagreement selection works.} With one file per unique image, the ordering reverses (Figure~\ref{fig:conditions}, bottom; Figure~\ref{fig:paired}, right). The 50 most-disputed unique images reach Dice 0.839, indistinguishable from the full set of 106 ($\Delta=-0.001$, $p=0.8$) while using less than half of it, above a random 50 in 11 of 15 replicates ($\Delta=+0.008$; Wilcoxon $p=0.048$, paired $t$ $p=0.07$), above the 50 most-agreed images by 2.6 points (14/15, $p<10^{-3}$), and above real-only data in every replicate. They also give the lowest seed sensitivity of any condition (0.007, half that of real-only training). The 50 most-agreed images and the middle band are both significantly worse than random and add little over real data alone. This is the classical query-by-committee ordering \citep{seung1992}: the images on which independently trained models disagree are the informative ones.

\begin{figure}[t]
\centering
\includegraphics[width=0.66\linewidth]{fig_scores.png}
\caption{Committee disagreement versus disagreement with the synthetic mask for the synthetic set (fold-0 committee), coloured by road fraction. Identical files overplot, so the 1{,}003 files appear as 93 points. Circles mark the 250 most-agreed (blue) and most-disputed (red) files.}
\label{fig:scores}
\end{figure}

\paragraph{What committee disagreement measures.} Figure~\ref{fig:scores} and the per-fold diagnostics show that committee disagreement is strongly correlated with label disagreement (Spearman rank correlation coefficient $\rho=0.86$--$0.89$ across folds): the images the committee disputes are those whose masks contain road structure that real-trained models do not yet predict. Given the deduplicated results, this is a signal of informativeness rather than of label noise. Disagreement is also negatively correlated with road density ($\rho=-0.25$ to $-0.47$), so the most-disputed subset under-represents dense-road scenes (mean road fraction 0.14--0.17 versus 0.20 for real data) and the most-agreed subset over-represents them (0.22--0.25); the most-disputed images win despite this shift. The ranking is a stable property of the images: on average 67\% of the 250 most-agreed files coincide across committees trained on different real folds. Why, then, did the most-disputed selection fail on the duplicated set? There it consisted of 26 unique images repeated about ten times each, and the deduplicated experiment shows separately that repetition of a fixed set is harmful under a matched budget (all-unique versus all). Narrow content plus heavy repetition, not the disagreement criterion, explains the earlier loss.

\section{Discussion}

Three practical rules follow. \emph{Audit before use}: a GAN-expanded dataset should be hashed for exact and near duplicates, because nominal size can overstate content by an order of magnitude, repetition itself costs accuracy under a fixed budget, and per-file selection or weighting rules behave pathologically on duplicated sets. \emph{Evaluate on real images only}: hold out real images before anything synthetic is added, cross-validate on real images, and match the optimization budget across conditions; otherwise gains are inflated many-fold, as our preliminary pooled-validation result shows. \emph{Select by disagreement, after deduplication}: a committee of models trained on the real data, which costs nothing beyond the seeds one should be running anyway, identifies a subset of synthetic images that delivers the full benefit of the set with less than half the images and the most stable training.

The modest size of the real benefit, about three Dice points from 106 unique synthetic images derived from 75 masks, bounds what Pix2Pix expansion offers here. How the 75 synthetic masks were produced is not documented; if, as their layouts suggest, they were derived from the 100 real masks, the generator's effective diversity is limited by construction. Whether a diffusion model or a larger pool of independent masks would help more is an open question that our protocol makes cheap to answer, and disagreement-based selection is most useful precisely when the candidate pool is large.

\paragraph{Limitations.} The study uses one benchmark of 100 real images, one generator whose synthetic masks were produced by a third party, one architecture and two subset sizes. Absolute Dice differs by fold (Figure~\ref{fig:conditions}), reflecting the small test set; our conclusions rest on paired within-fold comparisons, which are robust to this. The advantage of disagreement selection over random selection at equal size is modest ($+0.8$ Dice points) and significant by the Wilcoxon test but only marginally by the paired $t$-test; its advantages over agreement-based selection, over real-only training, and in seed sensitivity are large and unambiguous. Baseline U-Net was not re-evaluated under the new protocol. Duplicates were detected at the byte level; near-duplicate renderings of the same mask (29 masks have two) were kept as distinct images.

\section{Conclusion}

A public GAN-expanded road dataset advertised as 1{,}003 synthetic images contains 106, each repeated twelve times. Under a leakage-free, compute-matched, 15-replicate protocol those images improve Attention U-Net road segmentation on real held-out imagery by 3.3 Dice points, the repeated copies reduce that benefit, and validating on the pooled set inflates it sevenfold. Committee-disagreement selection of synthetic images fails on the duplicated set, where it collapses to a handful of unique images, but after deduplication the 50 most-disputed images match the full unique set, beat random and agreement-based selection, and halve seed sensitivity. Deduplicating synthetic sets, evaluating on real images only, and selecting synthetic images by committee disagreement should be default practice for GAN-expanded remote-sensing datasets.

\section*{Disclosure statement}
The author reports there are no competing interests to declare.

\section*{Funding}
No funding was obtained for the reported work.

\section*{Declaration of generative AI use}
In accordance with the Taylor \& Francis AI policy, the author discloses the use of the large language model Claude Fable 5.1 (Anthropic), accessed through the Claude Code interface, for two purposes: (i) language editing of the manuscript text, to improve clarity and grammar, and (ii) assistance in writing and debugging the experimental pipeline code, to reduce implementation time. The tool was not used to design the study, to generate or alter data or results, or to produce references; all reported numbers were produced by the released code, and the author reviewed and takes responsibility for all content.

\section*{Data availability statement}
The dataset is publicly available on Kaggle \citep{sanadalali}. Code, the frozen split file, committee scores and all per-run result files that support the findings are openly available at \repourl.

\begin{thebibliography}{99}

\bibitem[Barz and Denzler(2020)]{barz2020} Barz, B., and J. Denzler. 2020. ``Do We Train on Test Data? Purging CIFAR of Near-Duplicates.'' \emph{Journal of Imaging} 6 (6): 41. \url{https://doi.org/10.3390/jimaging6060041}.

\bibitem[Batra et al.(2019)]{batra2019} Batra, A., S. Singh, G. Pang, S. Basu, C. V. Jawahar, and M. Paluri. 2019. ``Improved Road Connectivity by Joint Learning of Orientation and Segmentation.'' In \emph{Proceedings of the IEEE/CVF Conference on Computer Vision and Pattern Recognition (CVPR)}. Long Beach, CA.

\bibitem[Buslaev et al.(2020)]{buslaev2020} Buslaev, A., V. I. Iglovikov, E. Khvedchenya, A. Parinov, M. Druzhinin, and A. A. Kalinin. 2020. ``Albumentations: Fast and Flexible Image Augmentations.'' \emph{Information} 11 (2): 125. \url{https://doi.org/10.3390/info11020125}.

\bibitem[Goodfellow et al.(2014)]{goodfellow2014} Goodfellow, I., J. Pouget-Abadie, M. Mirza, B. Xu, D. Warde-Farley, S. Ozair, A. Courville, and Y. Bengio. 2014. ``Generative Adversarial Nets.'' In \emph{Advances in Neural Information Processing Systems 27 (NIPS 2014)}, 2672--2680.

\bibitem[Isola et al.(2017)]{isola2017} Isola, P., J.-Y. Zhu, T. Zhou, and A. A. Efros. 2017. ``Image-to-Image Translation with Conditional Adversarial Networks.'' In \emph{Proceedings of the IEEE Conference on Computer Vision and Pattern Recognition (CVPR)}, 1125--1134.

\bibitem[Oktay et al.(2018)]{oktay2018} Oktay, O., J. Schlemper, L. Le Folgoc, M. Lee, M. Heinrich, K. Misawa, K. Mori, et al. 2018. ``Attention U-Net: Learning Where to Look for the Pancreas.'' In \emph{Medical Imaging with Deep Learning (MIDL)}. arXiv:1804.03999.

\bibitem[Ronneberger et al.(2015)]{ronneberger2015} Ronneberger, O., P. Fischer, and T. Brox. 2015. ``U-Net: Convolutional Networks for Biomedical Image Segmentation.'' In \emph{Medical Image Computing and Computer-Assisted Intervention -- MICCAI 2015}, Lecture Notes in Computer Science 9351, 234--241. Cham: Springer. \url{https://doi.org/10.1007/978-3-319-24574-4_28}.

\bibitem[sanadalali(n.d.)]{sanadalali} sanadalali. n.d. ``Satellite Images for Road Segmentation.'' Kaggle dataset. Accessed September 6, 2026. \url{https://www.kaggle.com/datasets/sanadalali/satellite-images-for-road-segmentation}.

\bibitem[Settles(2009)]{settles2009} Settles, B. 2009. \emph{Active Learning Literature Survey}. Computer Sciences Technical Report 1648. Madison, WI: University of Wisconsin--Madison.

\bibitem[Seung et al.(1992)]{seung1992} Seung, H. S., M. Opper, and H. Sompolinsky. 1992. ``Query by Committee.'' In \emph{Proceedings of the Fifth Annual Workshop on Computational Learning Theory (COLT)}, 287--294. New York: ACM. \url{https://doi.org/10.1145/130385.130417}.

\bibitem[Zhang et al.(2018)]{zhang2018} Zhang, Z., Q. Liu, and Y. Wang. 2018. ``Road Extraction by Deep Residual U-Net.'' \emph{IEEE Geoscience and Remote Sensing Letters} 15 (5): 749--753. \url{https://doi.org/10.1109/LGRS.2018.2802944}.

\bibitem[Zhou et al.(2018)]{zhou2018} Zhou, L., C. Zhang, and M. Wu. 2018. ``D-LinkNet: LinkNet with Pretrained Encoder and Dilated Convolution for High Resolution Satellite Imagery Road Extraction.'' In \emph{Proceedings of the IEEE Conference on Computer Vision and Pattern Recognition Workshops (CVPRW)}. Salt Lake City, UT.

\end{thebibliography}

\end{document}
